{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.session_id // \"nosess\"' | { read -r sid; mkdir -p .snapshots; m=\".snapshots/.session-$sid\"; if [ ! -f \"$m\" ]; then zip -qr \".snapshots/pre-$(date +%Y%m%d-%H%M%S)-$sid.zip\" . -x \"./.git/*\" -x \"./.snapshots/*\" 2>/dev/null && touch \"$m\"; fi; } 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "Saving app snapshot..."
          }
        ]
      }
    ]
  }
}
