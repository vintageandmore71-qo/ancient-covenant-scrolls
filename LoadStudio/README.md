# LoadStudio

Placeholder folder. The standalone PWA for LoadStudio will be dropped here.

When the bundle arrives:
- `index.html` (entry point)
- `sw.js` (service worker, cache namespace `loadstudio-vNN`)
- `manifest.json` (start_url `./`, scope `./`)
- `icon.png` (192×192 PWA icon)
- Asset folders (fonts/, lib-*, etc.)

Live URL once deployed: `https://dssorit.github.io/ancient-covenant-scrolls/LoadStudio/`
