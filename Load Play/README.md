# Load Play

Placeholder folder. The standalone PWA for Load Play will be dropped here.

When the bundle arrives:
- `index.html` (entry point)
- `sw.js` (service worker, cache namespace `load-play-vNN`)
- `manifest.json` (start_url `./`, scope `./`)
- `icon.png` (192×192 PWA icon)
- Asset folders

Live URL once deployed: `https://dssorit.github.io/ancient-covenant-scrolls/Load Play/`
