# WEEKLY HANDOFF — 2026-05-04 to 2026-05-10

Generated 2026-05-10. Full picture of what shipped, what was planned,
what remains, and what is awaiting iPad verification across all five
apps in the suite (ACR Reader, Load, LoadStudio, LoadPlay, Attain,
Attain Jr, Study).

## Cache versions live on `main` HEAD

| App         | Cache string                | Notes                                              |
|-------------|-----------------------------|----------------------------------------------------|
| ACR Reader  | (locked, do not edit)       | Root `/` app — no changes per CLAUDE.md rule 8     |
| Load        | `load-v17g76`               | Audio routing fix (2026-05-10)                    |
| LoadStudio  | `loadstudio-complete-v37`   | (no changes after 2026-05-04)                     |
| LoadPlay    | `loadplay-v32`              | (no changes after 2026-05-04)                     |
| Attain      | `attain-v72`                | Learning Quality Engine                            |
| Attain Jr   | `attainjr-v1s` (assumed)    | (no changes touched this week)                    |
| Study       | `acr-study-v80`             | Learning Quality Engine port + TTS wake-up        |

## Days at a glance

### 2026-05-04 — LoadStudio + LoadPlay + Load Main hardening
- Load Main `v17eb–v17el`: premium modern skin, comprehensive emoji
  strip → consistent SVG icon system, Editing Bay bottom-toolbar SVG
  icons, sidebar track-rail SVG icons, blue → purple recolor when
  running inside LoadStudio host, headless `?lsedit=` mode that hides
  Load Main UI when mounted as the LoadStudio editor, hoisted
  `window.openVideoEditor` so the editor is callable even if the rest
  of the IIFE throws.
- LoadStudio `v22–v37`: Editing Bay direct entry → Create / Upload
  chooser → fullscreen editor; Premium icon coverage; Install banner
  + How-to-install modal; Advertiser admin section + Creator Inbox
  section in Developer Lab; cross-suite footer pruned to Load-only;
  LoadStudio purple theme bound to `#__loadVideoEdit`. **Biggest
  deliverable:** Load Main's editor copied verbatim into
  `loadstudio/lseditor.js` (~870 KB, 17,867 lines) and mounted natively
  in the LoadStudio document — no iframe, no `../load/` runtime
  dependency.
- LoadPlay `v30–v32`: interactive guided tour (red theme), splash
  uncovered, install banner + how-to-install modal, cross-suite footer
  pruned, Reset Page Zoom auto-banner.
- Backup: `backup/2026-05-04-loadstudio-v37` → `4826b1b`.

### 2026-05-06 — Load Main handoff report (11 sections)
Eleven priority items from `inbox/Load_Main_Claude_Handoff_Report.zip`:

| # | Section | Version | Headline                                                  |
|---|---------|---------|-----------------------------------------------------------|
| 1 | 6       | v17fx   | Copy cleanup + AI/offline truth alignment                 |
| 2 | 7       | v17fy   | Feature Verification Dashboard 43 → 45 tests              |
| 3 | 10      | v17fz   | `lib-security-scanner.js` (BLOCKER/HIGH/MEDIUM/LOW/INFO)  |
| 4 | 11      | v17g0   | Receipt library aligned to Section 11 + 3 spec actions    |
| 5 | 8 + 9   | v17g1   | One-Click PWA Builder live build + ZIP integrity check    |
| 6 | 12      | v17g2   | LoadStudio package validator + 7 required UI actions      |
| 7 | 13      | v17g3   | `lib-rights-validator.js` 8-value enum + `blocksPublish`  |
| 8 | 14      | v17g4   | Strict-default sandbox + Trust Package flow               |
| 9 | 15      | v17g5   | Ecosystem routing aligned to spec wording                 |
| 10| 16      | v17g6   | Sample test projects (9 × 4 actions)                      |
| 11| 18+19   | v17g7   | `LOAD_MAIN_HANDOFF_FINAL_REPORT.md`                       |
| — | rem.    | v17g8   | Three plain-text dyslexia mentions in FAQ patched         |

Captured three NEW VERY IMPORTANT roadmap items: **X-VIDEO-AI**,
**X-DB**, **X-SUBS**. Backup: `backup/2026-05-06-v17g8`.

### 2026-05-07 — Branding, scaffold, Reliability Infrastructure (#1–17)
- Branding ™ pass: centralized config + manifests + runtime DOM
  rewriter, wired into all 84 sub-pages, source-level pass. `Load
  Eco™` surfaced as the ecosystem brand.
- LoadAI™ scaffold + cross-site navigation across Load Eco™
  (`f46259c`).
- Load `v17g33`: real on-device audio + video wired into Chat Studio.
- Load `v17g34–v17g35`: +14 free / open / local provider slots
  (Pollinations Flux/classic/Turbo, AI Horde, AI Horde SDXL, HF SDXL
  inpaint, Cloudflare AI, Cloudflare SDXL-Lightning, HF SDXL-Turbo,
  Together, Gemini Image, DeepAI, SiliconFlow, Real-ESRGAN, GFPGAN,
  CodeFormer, Local SD).
- Load `v17g36`: Chat Studio export project + regenerate / extend +
  5 metadata writers.
- **Reliability Infrastructure #1–17** (`v17g37–v17g53`):
  - #1 Provider Health Monitor
  - #2 Auto-Fallback Router for image
  - #3 Output Proof Gate library + audit (magic-byte WebM, RIFF/WAVE,
    OggS, ID3, ftyp, PK\x03\x04)
  - #4 Generation Queue
  - #5 Retry & Resume System
  - #6 Provider Timeout Rules (image 45s, i2i 60s, video 3–8 min,
    audio 60–120s)
  - #7 Local-only Mode
  - #8 Provider Budget / Cost Guard
  - #9 Asset Rights Inspector
  - #10 Scene Asset Dependency Checker + budget classification fixes
  - #11 Web Audio Scene Mixer
  - #12 Audio Cue Sheet / Timeline SFX
  - #13 FFmpeg / Audio-Video Muxing future path
  - #14 Model Capability Audit
  - #15 Prompt Compiler
  - #16 Continuity Lock System
  - #17 Versioned Generation History
- `v17g54–v17g55`: Piper paste-back capture, FAQ contradiction patch.
- `v17g56–v17g58`: image-edit + animate + Pollinations stuck-thinking
  fixes; shared AI creation pipeline (`LoadAIPipeline`); Image Prompt
  image-rescue when chat times out.

### 2026-05-08 — AI Chat Studio rebuild + Audio MVP + image-edit hardening
- `v17g59–v17g61`: full audio status labels + `.cinepwa.zip` export;
  audio MVP (import / record / voice-fx / preview / export); optional
  public-audio connectors (Freesound / Pixabay / OpenGameArt /
  Internet Archive / Wikimedia / Openverse) + procedural ambience
  (rain / wind / fire / room-tone / crowd-bed).
- `v17g62`: shared 17-provider chain extracted into
  `lib-load-image-providers.js` (Option B per `5.8 AI Studio Fix.docx`)
  + Simple Creator View. Image Prompt and AI Chat Studio import the
  same chain.
- `v17g63–v17g66`: AI Chat Studio result-area visibility patch,
  default mode = image (was chat — root cause of "POLLINATIONS.AI
  THINKING" hang), always-visible LIVE result panel, chat-timeline
  layout (sticky header, sticky bottom input bar, Tools popover,
  Advanced Details collapsed).
- `v17g67`: AI Chat Studio follow-up edit preserves source image
  (auto-stash result as `STATE.ref`, send() flips `image → i2i` when
  source + edit verb).
- `v17g68`: AI Chat Studio audio split — Voice / SFX / Ambience /
  Music. Voice-line guard rejects TTS-reading SFX descriptions.
- `v17g69`: honest labels — anatomy / camera-motion / audio quality
  gate.
- `v17g70`: edit-routing + voice/SFX guard + anatomy quality gate;
  `sfxLikeRx` guard in `runVoiceLine` refuses to TTS background-noise
  descriptions.
- `v17g71`: anatomy-safe edit refusal — refuses before provider call
  when no inpaint-class provider configured for hand/face/body edits.
- `v17g72`: non-anatomy follow-up edit no longer hangs (root cause:
  synchronous throw inside `imageGenWithFallback` escaped `.catch()`;
  fixed by `Promise.resolve().then(...)` wrap + 60s timeout).
- `v17g73`: non-anatomy edit — text-to-image opt-in fallback button.
- `v17g74`: in-chat edit-provider config + animal anatomy gate
  (`ANIMAL_QUALITY` clauses + `ANIMAL_RX` detection + Quality Issue
  Detected card with Retry / Refine / Save anyway / Advanced Details).
- `v17g75`: mask-painter wired to HF SDXL inpaint (true source-
  preserving edit). `openMaskPainter()` modal with brush slider,
  pointer + touch events, binary PNG mask saved to `STATE.editMask`,
  one-shot consumed after successful inpaint. `hardImg2Img: true`
  refuses text-to-image fallback for edit intents.

### 2026-05-09 — Study / Attain Learning Quality Engine + iPad TTS
- Study `v79`: iPad TTS wake-up. `speakText` / `playVerse` now do
  `resume() → cancel-if-busy → setTimeout(speak, 0)`; voices race
  waited up to 1.5s. Fixes sound dropping after silence.
- Attain `v72`: Learning Quality Engine (`lib-attain-quality.js`) —
  POS classifier with COMMON_VERBS / NOUNS / ADJS / ADVS lexicon,
  case parity, length parity, POS parity, number parity, tense parity
  (handles bring/sting/king as base, not progressive), blind-solve
  heuristic, `auditQuestion` / `auditQuestionSet` returning
  PASS / PARTIAL / FAIL + counts + reason frequency. Wired into
  `validateGenerated('multiple_choice')` in `attain-parser.js` and
  surfaced via Settings page audit panel. 9 self-test cases pass.
- Study `v80`: Learning Quality Engine ported to Study. Same
  `lib-attain-quality.js` copied into `study/`, wired via
  `passesQualityGate(q)` in `generateSmartQuestions` MC paths and
  Challenge Mode adapters. Audit panel added to `showProgress(fid)`
  between Backup & Restore and Back to activities.

### 2026-05-10 — Push-flow fix + workflow rules locked
- Diagnosed yesterday's `HTTP 403 / non-fast-forward` lockout:
  squash-merge of PR #4 rewrote `main`, leaving the long-running
  feature branch behind. Fixed by `git merge origin/main --no-edit`
  (no force push).
- PR #4 squash-merged → Load v17g74, v17g75, Study v79, v80, Attain
  v72 all on `main` as `91f7d39`.
- PR #5 squash-merged → CLAUDE.md auto-PR rule update.
- PR #7 squash-merged → CLAUDE.md sync-before-push rule + preferred
  shipping workflow.
- Load `v17g76` shipped to main (audio routing fix).
- "Allow auto-merge" enabled at the repo level.
- 11-step ship checklist locked into operating rules.

## Build plans captured this week (NOT YET STARTED)

These were specced from inbox files; no code shipped yet.

### Load Main Next Build Plan (13 parts)
1. Fix contradictory AI / privacy copy.  *(partially done v17fx)*
2. Feature Verification Dashboard.  *(done v17fy)*
3. LoadStudio package support `.loadstudio.zip` / `.cinepwa.zip`.
   *(done v17g2)*
4. Safety and Rights validation panel.  *(done v17g4)*
5. Export receipts after every export.  *(done v17g0)*
6. Ecosystem routing buttons.  *(done v17g5)*
7. Sample Projects section.  *(done v17g6)*
8. PWA Book Export standard.
9. Piper / TTS resilience improvements.  *(blocked on Stage 1 error)*
10. AI provider routing with no false positives.  *(reliability set)*
11. Module list (diagnostics, packages, sandbox, ai, voice, samples,
    ecosystem).
12. Code structure (diagnostic result format, validator, rights,
    receipt schemas).
13. 23 acceptance criteria.

### PWA One-Click Create (Parts 14–17)
- One-click Build Standalone PWA ZIP button.  *(done v17g1)*
- 20-step build flow with real-time progress.
- Security scanner for every generated ZIP.  *(done v17fz)*
- 18 acceptance criteria.

### Load Main AI Addendum — X-AI-AUDIO Sound and Atmosphere Engine
- Three sound paths: embedded video audio, separate generated
  audio/SFX, sound prompt fallback.
- 10 audio status labels.
- 14 new scene fields: `audioPrompt / sfxPrompt / musicPrompt /
  ambiencePrompt / voicePrompt / audio / music / sfx[] / audioStatus
  / audioProvider / audioOutputProof / audioEmbedded / audioMuxed /
  audioRightsStatus`.
- 5 chat card types: animation request, animation progress, audio
  generation, video result, audio result.
- Stage path: prompt extraction → separate gen → layered playback →
  FFmpeg.wasm muxing → embedded audio.
- *(audio MVP shipped v17g60–v17g61; full Sound and Atmosphere
  Engine schema NOT yet wired)*

### 5.8 AI Studio Fix.docx + 5.8 READ AI Studio Not working.docx
- Part 1: Unify image system — shared `lib-load-image-providers.js`.
  *(done v17g62)*
- Part 2: AI Chat Studio result area must always show
  loading/success/failure card.  *(done v17g65)*
- Part 3: Follow-up edit must stay grounded in previous image.
  *(done v17g67, hardened v17g70–v17g75)*
- Part 4: Image quality / anatomy enforcement.  *(done v17g69–v17g74)*
- Part 5: Audio / SFX / voice honest labels.  *(done v17g68–v17g70)*
- Part 6: Animation honest labeling (camera zoom not character
  animation).  *(done v17g69)*
- Part 7: Simplify AI Chat Studio UI.  *(done v17g66 chat-timeline)*
- Part 8: Keep both surfaces, connect them correctly.  *(done v17g62)*
- Part 9: Preserve package / output proof rules.  *(done; reliability
  set)*
- Part 10: Provider timeout / fallback — no silent failure.  *(done
  v17g42 + v17g72)*
- Part 11: 9 test cases.  *NOT YET RUN ON iPad.*

### Reliability Infrastructure Addendum (1–6)
All 6 items shipped this week as `v17g37–v17g42`. Full library lives
in `load/lib-*.js` files.

### Attain Learning Engine Rebuild (Phases 1–4)
- Phase 1: Distractor parity + blind-solve gate.  *(done Attain v72,
  Study v80)*
- Phase 2: Non-MC question types — Speaker quiz, True-False, Rhyme
  groups, matching, ordering, paraphrase identification, concept
  grouping, context interpretation, thematic connections, memory
  reconstruction.  *NOT STARTED.*
- Phase 3: Difficulty tiers (easy / medium / hard / expert),
  spaced-repetition adaptive frequency tuning beyond SM2, weak-area
  detector, confidence tracking.  *NOT STARTED.*
- Phase 4: 100-question acceptance test runner with 90 / 90 bar.
  *NOT STARTED.*

## Open VERY IMPORTANT roadmap items (no code yet)

| Code            | Item                                                  | Blocker / dependency                                  |
|-----------------|-------------------------------------------------------|-------------------------------------------------------|
| X-VIDEO-AI      | Advanced AI video generation                          | Open scope: provider chain, key UI, video receipt     |
| X-AI-AUDIO      | Sound and Atmosphere Engine                           | Schema captured, not wired                            |
| X-DB            | Full production database                              | Open scope: vendor / auth / conflict / GDPR           |
| X-SUBS          | Subscription system                                   | Blocks on X-DB                                        |
| X-AI-PROVIDERS  | Free / open / local-first provider strategy + registry| 14-question + keep/extend audits required first       |
| X-AI-14         | Tier-14 Glam-parity AI add-ons (FaceFix / ESRGAN /    | Optional layer on top of shipped X-AI-CORE             |
|                 | SiliconFlow / HF Spaces / I2V / AI Style Chat)        |                                                       |
| X-B2V           | Book-to-Video wiring                                  | 8 JS files dropped at `load/book-video/`, not loaded  |
| X-V2V           | Verse-to-Video                                        | Reuses X-B2V pipeline                                 |
| X-P2V           | Photo-to-Video promotion                              | Partial inside LoadStudio Editing Bay                 |
| X-CC            | Character Consistency module                          | Shipped v17ex; not yet wired into B2V / Studio        |
| X-PIPER         | Piper TTS Stage 1 unblock + Stage 2                   | Blocked on user reporting visible play() error        |

## Awaiting iPad verification (NEVER claimed live)

| App     | Version    | Test                                                                                  |
|---------|-----------|---------------------------------------------------------------------------------------|
| Load    | v17g75    | Mask-painter + HF SDXL inpaint round-trip — see SESSION_NOTES_2026-05-09 for steps    |
| Load    | v17g74    | Animal anatomy gate (Quality Issue Detected card)                                     |
| Load    | v17g76    | Audio routing fix                                                                     |
| Load    | (live)    | AI Chat Studio + Image Prompt working end-to-end (flagged broken 2026-05-10)          |
| Attain  | v72       | Learning Quality Audit — Settings → "Run Learning Quality Audit"                      |
| Study   | v80       | Learning Quality Audit — Progress → purple-bordered audit card                        |
| Study   | v79       | TTS wake-up after silence (PASS confirmed once; re-verify)                            |
| LoadStudio | v37    | Editing Bay end-to-end smoke (Upload, Create from Scratch, toolbar visibility)        |
| LoadPlay| v32       | Zoom banner sanity check                                                              |

## Backups available

- `backup/2026-04-28-v17cs`
- `backup/2026-05-04-loadstudio-v37` → `4826b1b`
- `backup/2026-05-06-v17g8`
- (no backup created yet for the v17g76 / v80 / v72 stable point —
  the next session that confirms iPad PASS should create
  `backup/2026-05-10-v80-attain-v72-load-v17g76`)

Recovery: `git checkout backup/<name>`.

## Operating-rules changes locked this week (CLAUDE.md)

1. **Rule 6** rewritten: shipping push flow uses PR + auto-merge, not
   direct main push.
2. **Rule 7** added: sync from `origin/main` before every push and at
   session start (`git fetch origin main && git merge origin/main
   --no-edit`).
3. **Rule 8** renumbered: ACR Reader lock unchanged.
4. New "Preferred shipping workflow" section: fresh branch per
   shipping unit, no force push, no live-complete claim until iPad
   confirms the new cache marker.
5. 11-step pre-ship checklist locked.

## Capability gaps observed all week

- `dssorit.github.io` unreachable from sandbox; live-Pages
  verification always requires user iPad confirmation.
- iPad-only behavior (Safari speechSynthesis idle state, MediaRecorder,
  HF inpaint round-trip) cannot be tested from sandbox.
- No MCP tool exposes repo Rulesets; branch-protection state must be
  inferred from verbatim push errors.

## What to pick up first next session

1. **Verify iPad acceptance walk** for everything in the table above —
   nothing in this week's ship is "live" until you confirm it.
2. **AI Chat Studio + Image Prompt end-to-end test** (flagged broken
   on 2026-05-10 in `SESSION_NOTES_2026-05-10.md`). Run the 11-test
   plan from `5.8 READ AI Studio Not working.docx` Part 11.
3. **Pick one VERY IMPORTANT roadmap item to scope:** recommended
   order is X-AI-PROVIDERS audits → X-VIDEO-AI + X-AI-AUDIO together
   → X-DB → X-SUBS.
4. **Attain Phase 2** (non-MC question types) once Phase 1 is iPad-
   verified.
